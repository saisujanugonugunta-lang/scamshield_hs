{
  "name": "scamshield-railway",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "build": "npm run build --prefix frontend && rm -rf backend/frontend-dist && cp -r frontend/dist backend/frontend-dist",
    "start": "node backend/server.js",
    "dev": "npm run dev --prefix frontend"
  }
}
